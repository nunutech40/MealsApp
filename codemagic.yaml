workflows:
  ios_ci:
    name: iOS CI (auto-detect path)
    max_build_duration: 60
    environment:
      xcode: latest

    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
          source: true

    scripts:
      - name: Debug layout
        script: |
          echo "PWD=$(pwd)"
          echo "=== Struktur repo ==="
          # Mencari file proyek saja, mengabaikan xcworkspace dan Podfile
          find . -maxdepth 2 -type f -name "*.xcodeproj"

      - name: Build for iOS Simulator (auto detect)
        script: |
          set -euo pipefail
          # CARI HANYA FILE .XCODEPROJECT
          PROJECT_PATH="$(find . -maxdepth 3 -name '*.xcodeproj' | head -n1 || true)"
          
          # MENGHAPUS LOGIKA WORKSPACE_PATH

          if [ -z "$PROJECT_PATH" ]; then
            echo "❌ Tidak menemukan .xcodeproj."
            exit 1
          fi

          SCHEME_NAME="MealsApp"
          BUILD_COMMAND="-project" # Memaksa penggunaan -project

          echo "Project : $PROJECT_PATH"
          echo "Scheme  : $SCHEME_NAME"

          # HANYA SATU BLOK XCODEBUILD (MEMAKSAKAN -project)
          xcodebuild \
            "$BUILD_COMMAND" "$PROJECT_PATH" \
            -scheme "$SCHEME_NAME" \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,OS=latest,name=iPhone 16' \
            clean build | xcpretty
          # MENGHAPUS BLOK 'if/else'

    artifacts:
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.app
      - $HOME/Library/Logs/**/*
    publishing:
      email:
        recipients:
          - nunutech4.0@gmail.com
        notify:
          success: false
          failure: true
